name: lachies_life_planner
description: All-in-1 personal planner
version: 1.0.0+5

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  lachies_component_library:
    git:
      url: https://github.com/lachiejames/lachies_component_library
      ref: master
  # lachies_component_library:
  #   path: ../lachies_component_library

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0
  screenshots: any
  test: any

flutter:
  uses-material-design: true
