name: lachies_life_planner
description: All-in-1 personal planner
version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0
  screenshots: any
  test: any

flutter:
  uses-material-design: true
